# IMX662 IPA Configuration - DISABLED (not using ISP)
# Uncomment below if you want to re-enable ISP in the future

# if(CONFIG_CAMERA_IMX662)
#     idf_build_set_property(ESP_IPA_JSON_CONFIG_FILE_PATH "${CMAKE_CURRENT_LIST_DIR}/cfg/imx662_default.json" APPEND)
# endif()
